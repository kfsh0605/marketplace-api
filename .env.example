# Порт, на якому запускається HTTP-сервер
PORT=3000

# Середовище виконання: development | test | production
NODE_ENV=development

# Підключення до PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=marketplace
DB_NAME=marketplace